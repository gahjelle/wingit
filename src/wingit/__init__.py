"""wingit: run your agent harness in headless mode behind a thin, pipeable CLI."""


def main() -> None:
    """Entry point for the `a` / `wingit` command (placeholder)."""
    # Placeholder entry point; real dispatch lands test-first per docs/agents/.
    print("Hello from wingit!")  # noqa: T201
